# AI智能招聘管理系统 - 运行指南

## 一、环境要求

### 已安装的依赖
- ✅ .NET 9 SDK (系统已有)
- ✅ Redis 3.0.504 (端口 6379)
- ✅ SQL Server Express (localhost\SQLEXPRESS)
- ✅ SSMS 21 (已安装)

### 可选依赖
- RabbitMQ (消息队列，可选)

---

## 二、配置数据库

### 1. 使用 SSMS 创建数据库

1. 打开 **SQL Server Management Studio 21**
2. 连接服务器: `localhost\SQLEXPRESS`
3. 身份验证: **Windows 身份验证**
4. 新建查询，执行:

```sql
CREATE DATABASE AIRecruitment;
GO
```

### 2. 配置后端连接字符串

文件: `Backend/AIRecruitment.Api/appsettings.json`

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost\\SQLEXPRESS;Database=AIRecruitment;Trusted_Connection=True;TrustServerCertificate=True",
    "Redis": "localhost:6379"
  }
}
```

---

## 三、运行项目

### 方式一：仅运行前端（推荐用于演示）

前端已配置好，可直接运行:

```bash
cd C:/Users/Lenovo/WorkBuddy/20260417091104
npm run dev
```

访问地址: http://localhost:3000

### 方式二：完整运行（前后端联调）

#### 1. 启动 Redis

Redis 已安装为 Windows 服务，应该自动运行。检查状态:

```powershell
# 检查 Redis 是否运行
powershell -Command "& 'C:\Program Files\Redis\redis-cli.exe' ping"
# 应返回: PONG
```

如未运行，手动启动:
```powershell
Start-Process -FilePath "C:\Program Files\Redis\redis-server.exe" -WindowStyle Hidden
```

#### 2. 启动后端 API

```bash
cd C:/Users/Lenovo/WorkBuddy/20260417091104/Backend/AIRecruitment.Api
dotnet run --urls "http://localhost:5000"
```

后端启动后会自动创建数据库表结构。

#### 3. 启动前端

```bash
cd C:/Users/Lenovo/WorkBuddy/20260417091104
npm run dev
```

---

## 四、访问系统

| 功能 | 地址 |
|------|------|
| 前端首页 | http://localhost:3000 |
| 岗位列表 | http://localhost:3000/jobs |
| 登录页面 | http://localhost:3000/login |
| HR工作台 | http://localhost:3000/admin/dashboard |
| 岗位管理 | http://localhost:3000/admin/jobs |
| 简历管理 | http://localhost:3000/admin/resumes |
| AI招聘中心 | http://localhost:3000/admin/ai-center |
| 面试管理 | http://localhost:3000/admin/interviews |
| 数据统计 | http://localhost:3000/admin/statistics |

后端 API 文档: http://localhost:5000/swagger

---

## 五、测试账号

首次运行需要注册账号:

1. 访问 http://localhost:3000/register
2. 选择角色:
   - **HR** - 招聘方，可发布岗位、管理简历
   - **求职者** - 可浏览岗位、投递简历

---

## 六、常见问题

### Q1: 前端启动失败
```bash
# 重新安装依赖
npm install
npm run dev
```

### Q2: 后端编译失败
```bash
cd Backend/AIRecruitment.Api
dotnet restore
dotnet build
```

### Q3: 数据库连接失败
- 确认 SQL Server Express 服务已启动
- 检查连接字符串中的服务器名称
- 尝试使用 Windows 身份验证

### Q4: Redis 连接失败
- 确认 Redis 服务已启动: `redis-cli ping`
- 检查端口 6379 是否被占用

---

## 七、项目结构

```
AIRecruitment/
├── 前端 (Vue 3 + Vite + Element Plus)
│   ├── src/
│   │   ├── api/          # API 接口封装
│   │   ├── views/        # 页面组件
│   │   │   ├── public/   # 求职者端
│   │   │   └── admin/    # HR管理后台
│   │   ├── stores/       # Pinia 状态管理
│   │   └── router/       # 路由配置
│   └── package.json
│
└── 后端 (.NET 8 + EF Core)
    └── AIRecruitment.Api/
        ├── Controllers/    # API 控制器
        ├── Services/       # 业务服务层
        ├── Models/         # 数据实体
        └── Program.cs      # 入口配置
```

---

## 八、技术栈

| 层级 | 技术 |
|------|------|
| 前端 | Vue 3, Vite, Element Plus, Pinia, ECharts |
| 后端 | .NET 8, EF Core, JWT, SignalR |
| 数据库 | SQL Server, Redis |
| 消息队列 | RabbitMQ (可选) |
| AI | 大模型 API (预留接口) |

---

## 九、联系方式

如有问题，请查看项目文档或联系开发团队。